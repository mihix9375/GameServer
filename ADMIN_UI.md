# Server管理画面

Server起動時に、gRPCサーバーと一緒に管理画面が起動します。

## Server PCから使う

既定では次のURLで開きます。

```text
http://127.0.0.1:50051
```

初回起動時、`Server.exe` と同じフォルダーに `admin-config.json` が自動作成されます。

```json
{
  "bind": "127.0.0.1:50051",
  "token": "",
  "leaderboard_bind": "0.0.0.0:50052"
}
```

この設定ではServer PC以外から管理画面へ接続できません。

## 部内LANから使う

Serverを停止してから `admin-config.json` を次のように変更し、再起動します。

```json
{
  "bind": "0.0.0.0:50051",
  "token": "推測されにくい長い管理トークン",
  "leaderboard_bind": "0.0.0.0:50052"
}
```

別PCのブラウザーから `http://ServerのIPアドレス:50051` を開き、設定した管理トークンを入力します。

LAN公開時にトークンが空の場合、gRPCサーバーは動作しますが管理画面は安全のため起動しません。Windows FirewallでTCP 50051を許可する作業は必要に応じて管理者が行ってください。

`leaderboard_bind` はLauncherから接続されるランキングAPIです。既定ではLAN内のLauncherから使えるようTCP 50052で待ち受けます。Windows FirewallでTCP 50052の受信を許可してください。Unityゲームがこのポートへ直接接続する必要はありません。

## 操作

- ServerとgRPCエンドポイントの稼働状況確認
- 配布ゲーム、バージョン、保存先の確認
- ZIPファイルから新しいゲームをアップロード
- 同じゲームIDのZIPをアップロードしてゲーム本体を更新
- タイトル、作者、バージョン、更新日、タグ、説明の変更
- 配布ZIP内の `meta.json` を含めたゲーム情報の更新
- ゲームと関連コメントの削除
- `temp` フォルダーと配布ゲームの再スキャン
- 選択したゲームの更新通知をLauncherへ送信
- ゲーム別コメントの確認、検索、削除
- ゲームごとに最大2つのランキングを設定
