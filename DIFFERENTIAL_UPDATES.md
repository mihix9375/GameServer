# 差分アップデート

ゲーム更新はファイル単位のSHA-256マニフェストを使います。

## 更新の流れ

1. Launcherが`GetGameManifest`で最新バージョンのファイル一覧を取得します。
2. インストール済みの`.gamelauncher-manifest.json`と比較します。
3. 追加・変更されたファイルだけを`DownloadGameFiles`で取得します。
   ただし変更ファイルの非圧縮合計が配布ZIP以上なら、帯域を節約するため完全ZIPへ自動で切り替えます。
4. 変更されていないファイルは一時ステージへハードリンクし、利用できない場合だけコピーします。
5. 受信したファイルのサイズとSHA-256を検証します。
6. `meta.json`、ゲームID、バージョン、実行ファイルを検証してから旧版と入れ替えます。
7. 失敗した場合は旧版へ戻します。新マニフェストにないファイルは新ステージへコピーされないため削除扱いになります。

初回インストールと上記の切替時は完全ZIPを使用します。完全ZIPは最大4ワーカーで並列展開され、展開後に全ファイルをマニフェストと照合します。LauncherはgRPC Channelを再利用し、各操作のたびに新しい接続を作りません。

## Server側

Serverは配布ZIPからマニフェストを生成し、ZIPのサイズと更新日時が変わるまでメモリ上で再利用します。同時に複数台が更新しても、各Launcherには必要なファイルだけが2MiBチャンクで送られます。

## 互換性

従来の`DownloadGame` RPCは初回インストール用として維持しています。差分更新には次のRPCを追加しています。

- `GetGameManifest`
- `DownloadGameFiles`
